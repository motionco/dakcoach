/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "firebasestorage.googleapis.com", "drive.google.com", "thumb2.photo.mybox.naver.com"]
  },
};

module.exports = nextConfig;
